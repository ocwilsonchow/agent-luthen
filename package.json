{
  "name": "agent-luthen",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "sso": "aws sso login --sso-session=sinlongchow",
    "auth:generate": "bunx sst shell --stage local -- bun run --filter @repo/auth auth:generate",
    "db:generate": "bunx sst shell --stage local -- bun run --filter @repo/db db:generate",
    "db:migrate": "bunx sst shell --stage local -- bun run --filter @repo/db db:migrate",
    "db:push": "bunx sst shell --stage local -- bun run --filter @repo/db db:push",
    "db:studio": "bunx sst shell --stage local -- bun run --filter @repo/db db:studio",
    "mastra:studio": "bash scripts/mastra-studio.sh",
    "setup:schema": "bun run auth:generate && bun run db:generate && bun run db:migrate && bun run db:push",
    "dev": "bunx sst dev --stage local",
    "deploy:dev": "bunx sst deploy --stage dev",
    "killports": "bash scripts/kill-ports.sh"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.162",
    "prettier": "^3.7.4",
    "turbo": "^2.10.10",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "devEngines": {
    "packageManager": {
      "name": "bun",
      "version": "1.3.14",
      "onFail": "ignore"
    }
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@repo/infra": "workspace:*",
    "sst": "4.17.1"
  },
  "overrides": {
    "better-auth": "1.7.1",
    "@better-auth/core": "1.7.1"
  }
}
